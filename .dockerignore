.git
.editorconfig
node_modules
vendor
storage/*.key
.phpunit.result.cache
docker-compose.yml
Dockerfile
.env
# Exclude the storage symlink - it's a symlink to ../storage/app/public
# which Docker cannot resolve. The entrypoint runs `php artisan storage:link` instead.
public/storage